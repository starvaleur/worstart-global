kind = "web"
previewPath = "/"
title = "WORSTART SHIPPING"
description = "The official corporate website for WORSTART SHIPPING, a logistics and shipping layer for freight, fulfillment, documentation and shipment visibility across Algeria, China, the European Union, Türkiye, the United Arab Emirates and the United States."
version = "1.0.0"
id = "artifacts/website"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19161

[services.development]
run = "pnpm --filter @workspace/website run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/website", "run", "build" ]
serve = "static"
publicDir = "artifacts/website/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19161"
BASE_PATH = "/"
